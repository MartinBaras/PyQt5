ventana = QWidget()  # Crea el widget principal (ventana)

self.setWindowTitle("Registro de Usuario") # Titulo ventana

self.setGeometry(100, 100, 400, 300) # Posición (x, y) y tamaño ventana (ancho, alto)

layout = QGridLayout() # Crear Grid Layout (Organiza los widgets en filas y columnas)

self.setLayout(layout) # Aplicar Grid Layout a la ventana

nombre = QLabel("Nombre:") # Crear etiqueta

inputbox_nombre = QLineEdit() # Crear inputbox para ingresar texto

layout.addWidget(self.subtitulo,0,0,0,0) # Agregar WIDGET a la ventana (self.WIDGET, Fila, Columna, Fila Spanning, Columna Spanning)

self.subtitulo.setAlignment(Qt.AlignHCenter) # Alinear etiqueta al centro hacia arriba

self.subtitulo.setFont(QFont("Arial", 16, QFont.Bold)) # Establecer fuente "Arial", Tamaño 16, Negrita

self.inputbox_contraseña.setEchoMode(QLineEdit.Password) # Oculta el texto del input box.

self.genero_masculino = QRadioButton("Masculino") # Permite seleccionar una opción.

self.comboboxpais = QComboBox() # QComboBox permite elegir una opción de una lista desplegable.

self.comboboxpais.addItem("Argentina") # Agregar item al QComboBox

self.comboboxpais.addItems(["Argentina","Chile"]) # Agregar varios items al QComboBox en una sola linea

self.terminos = QCheckBox("Acepto los términos y condiciones") # QCheckBox permite marcar y desmarcar una casilla.

# QPushButton ejecuta una función al hacer clic.

# QDateEdit permite seleccionar una fecha.

# QSpinBox permite elegir un número (Ej: cantidad de pasajeros).
